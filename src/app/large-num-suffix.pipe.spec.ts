import { LargeNumSuffixPipe } from './large-num-suffix.pipe';

describe('LargeNumSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new LargeNumSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
